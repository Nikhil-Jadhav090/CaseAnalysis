# Case Analysis — Frontend

This is a React + Vite + Tailwind frontend scaffold for the Case Analysis system.

Quick start (Windows PowerShell):

```powershell
cd d:/CaseAnalysis/frontend
npm install
npm run dev
```

What is included:
- Modern React (v18) + Vite project
- Tailwind CSS configured
- Pages: Landing, Login, Register, Dashboard, Case create/list/details, AI Chat
- Components: Navbar, Footer

Next steps:
- Wire to backend API endpoints (auth, cases, AI analysis)
- Add charts (e.g., Chart.js or Recharts) for analytics
- Implement PDF export/report generation using a library like jsPDF or Puppeteer server-side
- Protect routes and add role-based UI for Admin/Analyst/User

If you want, I can now:
- Wire the UI to a mock backend (JSON server) or real API skeleton
- Add unit tests and E2E smoke tests
